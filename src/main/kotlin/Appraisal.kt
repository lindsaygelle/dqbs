package com.github.lindsaygelle

import java.util.*

class Appraisal(assessments: List<Assessment>, result: Boolean, timeMilliseconds: Long, uuid: UUID) : Consideration(
    assessments, result, timeMilliseconds, uuid
)