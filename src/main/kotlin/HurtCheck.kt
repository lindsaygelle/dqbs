package com.github.lindsaygelle

import java.util.*

class HurtCheck(magicPoints: Int, magicPointsRequirement: Int, timeMilliseconds: Long, uuid: UUID) : MagicCheck(
    magicPoints,
    magicPointsRequirement,
    timeMilliseconds,
    uuid
) {
    // TODO
}