package com.github.lindsaygelle

import java.util.*

class SleepCheck(
    magicPoints: Int, magicPointsRequirement: Int, result: Boolean, timeMilliseconds: Long, uuid: UUID
) : MagicCheck(
    magicPoints, magicPointsRequirement, result, timeMilliseconds, uuid
)